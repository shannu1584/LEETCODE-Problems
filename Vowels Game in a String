class Solution(object):
    def doesAliceWin(self, s):
        """
        :type s: str
        :rtype: bool
        """
        vowels = set("aeiou")
        for ch in s:
            if ch in vowels:
                return True  # Alice has a valid move
        return False  # no vowels → Alice cannot start
